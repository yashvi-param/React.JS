// calculating array length

let veg = [
    "carrot", 
    "tomato", 
    "onion", 
    "cabbage", 
    "potato", 
    "ladyfinger"
];

console.log("veg length: ", veg.length);

//increase and decrease the aaray length

console.log("veg element and their length before: ", veg.length)

//increase length

veg.length = 10;

console.length(
"veg element and its length after increasing: ",
  veg.length,
  veg
);

// decrease length

veg.length = 4;

console.log(
 "veg element and its length after decreasing: ",
  veg.length,
  veg
);